import { IndCurrencyPipe } from './ind-currency.pipe';

describe('IndCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new IndCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
